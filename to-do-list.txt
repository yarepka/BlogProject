1. Log In and Sign Up should create user in DB
2. Create Communities seeder (create near 30 communities)
3. Create near 40 posts for each community 40*30=1200 posts,
most of it will be lorem ipsum with lorem ipsum image, but some (~2 posts) should be real posts with real title, real text and image which will be stored in the file system.

A) Check if username is already exists
B) Make redirect to the new page after registered
C) Server add additionalHeaders to all functions